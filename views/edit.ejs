<form action="/admin/edit/<%= post._id %>" method="POST" enctype="multipart/form-data"
  class="bg-white dark:bg-gray-800 p-6 rounded shadow space-y-6">

  <!-- Title -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">Title</label>
    <input type="text" name="title" value="<%= post.title %>" required
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white" />
  </div>

  <!-- Author -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">Author</label>
    <input type="text" name="author" value="<%= post.author || '' %>" placeholder="Author name"
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white" />
  </div>

  <!-- Category Dropdown -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">Category</label>
    <select name="category" required
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white">
      <% const categories = ['Technology', 'Lifestyle', 'News', 'Business', 'Entertainment']; %>
      <% categories.forEach(cat => { %>
        <option value="<%= cat %>" <%= post.category === cat ? 'selected' : '' %>><%= cat %></option>
      <% }) %>
    </select>
  </div>

  <!-- YouTube Video URL -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">YouTube Video URL (optional)</label>
    <input type="url" name="videoUrl" value="<%= post.videoUrl || '' %>"
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white" />
  </div>

  <!-- Post Content -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">Post Content (HTML supported)</label>
    <textarea name="content" rows="10" required
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white"><%= post.content %></textarea>
  </div>

  <!-- Existing Media Preview -->
  <% if (post.media) { %>
    <div>
      <p class="text-sm text-gray-500 dark:text-gray-400 mb-1">Current Media:</p>
      <img src="<%= post.media %>" alt="Post Media"
        class="w-full h-auto max-h-64 object-contain rounded border" />
    </div>
  <% } %>

  <!-- Optional Media Upload -->
  <div>
    <label class="block font-medium mb-1 text-gray-800 dark:text-gray-200">Replace Media (optional)</label>
    <input type="file" name="media"
      class="w-full p-2 border rounded bg-gray-50 dark:bg-gray-900 dark:text-white" />
  </div>

  <!-- Action Buttons -->
  <div class="flex justify-between">
    <a href="/admin" class="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700 transition">Cancel</a>
    <button type="submit"
      class="px-6 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition">💾 Save Changes</button>
  </div>
</form>
