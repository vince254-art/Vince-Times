<section class="hero-section">
  <div class="hero-content">
    <h1 class="hero-title">Welcome to VinceTimes</h1>
    <p class="hero-subtitle">Your trusted source for uncensored journalism, tech insights, and more.</p>
    <a href="#latest" class="hero-button">Explore Latest Stories</a>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    padding: 6rem 2rem;
    text-align: center;
    background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.7)), url('/images/hero-bg.jpg') center/cover no-repeat;
    color: var(--hero-text, #fff);
  }

  html.dark .hero-section {
    --hero-text: #f0f0f0;
  }

  html:not(.dark) .hero-section {
    --hero-text: #f9f9f9;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    animation: fadeInUp 1.2s ease forwards;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: inherit;
  }

  .hero-subtitle {
    font-size: 1.3rem;
    margin-bottom: 2rem;
    line-height: 1.6;
    color: inherit;
  }

  .hero-button {
    background-color: #2563eb;
    color: #fff;
    font-weight: bold;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    display: inline-block;
    transition: background-color 0.3s ease, transform 0.2s ease;
  }

  .hero-button:hover {
    background-color: #1e40af;
    transform: translateY(-2px);
  }

  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }
  }
</style>
